# gator
You'll need Postgres and Go installed to run the program.

You'll also need to know how to install the program.

Finally, you'll need to know how the program is written,
as I'm not documenting it at all.

But rather, don't attempt to install this program, since
it will only work terribly.